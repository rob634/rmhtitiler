# Production dependencies for TiTiler with Azure OAuth authentication
# These are installed in addition to the base TiTiler image

# Azure authentication - OAuth tokens via Managed Identity
# Note: azure-storage-blob is NOT needed for OAuth approach
azure-identity>=1.15.0
